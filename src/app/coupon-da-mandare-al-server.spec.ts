import { CouponDaMandareAlServer } from './coupon-da-mandare-al-server';

describe('CouponDaMandareAlServer', () => {
  it('should create an instance', () => {
    expect(new CouponDaMandareAlServer()).toBeTruthy();
  });
});
