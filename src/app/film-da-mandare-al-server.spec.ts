import { FilmDaMandareAlServer } from './film-da-mandare-al-server';

describe('FilmDaMandareAlServer', () => {
  it('should create an instance', () => {
    expect(new FilmDaMandareAlServer()).toBeTruthy();
  });
});
